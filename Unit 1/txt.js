let a=[250,325,450];
for(let i=0;i<a.length;i++)
{
    let offer=a[i]/10;
    a[i]=a[i]-offer;
    console.log(`value after offer =${a[i]}`);
}


let obj={
    name:'xyz',
    roll:1
}
array.forEach(element => {
    console.log(element);
});